---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../base
  - cluster-windmill-db.yaml
  - onepassworditem-oidc.yaml
patches:
  - path: values.yaml
