---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../base
  # MCP server deployments
  - deployment-github-mcp.yaml
  - deployment-grafana-mcp.yaml
  - deployment-terraform-mcp.yaml
  - deployment-context7-mcp.yaml
  # Agentgateway configuration
  - gateway.yaml
  - httproute.yaml
  - agentgatewaybackend-mcp-federation.yaml
  - ingress-mcp.yaml
