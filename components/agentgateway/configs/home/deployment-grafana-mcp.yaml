---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: grafana-mcp
  namespace: agentgateway
  labels:
    app: grafana-mcp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: grafana-mcp
  template:
    metadata:
      labels:
        app: grafana-mcp
    spec:
      containers:
        - name: grafana-mcp
          image: ghcr.io/bendwyer/containers/grafana-mcp:1.3@sha256:f2a5fd4e8a02a85623891d75039832bbc402b04c1858cabf876f7a9b178dccc6
          args:
            - --transport
            - streamable-http
            - --address
            - 0.0.0.0:8000
            # Accept any Host header. Only the gateway can reach this service.
            - --allowed-hosts
            - "*"
          env:
            - name: GRAFANA_URL
              value: "http://grafana.grafana.svc.cluster.local"
            - name: GRAFANA_SERVICE_ACCOUNT_TOKEN
              valueFrom:
                secretKeyRef:
                  name: grafana-mcp
                  key: credential
          ports:
            - containerPort: 8000
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              memory: 128Mi
---
apiVersion: v1
kind: Service
metadata:
  name: grafana-mcp
  namespace: agentgateway
  labels:
    app: grafana-mcp
spec:
  selector:
    app: grafana-mcp
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      appProtocol: agentgateway.dev/mcp
  type: ClusterIP
