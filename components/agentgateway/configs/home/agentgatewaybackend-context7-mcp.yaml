---
apiVersion: agentgateway.dev/v1alpha1
kind: AgentgatewayBackend
metadata:
  name: context7-mcp
  namespace: agentgateway
spec:
  mcp:
    targets:
      - name: context7-mcp
        selector:
          services:
            matchLabels:
              app: context7-mcp
