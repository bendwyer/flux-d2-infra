---
apiVersion: agentgateway.dev/v1alpha1
kind: AgentgatewayBackend
metadata:
  name: grafana-mcp
  namespace: agentgateway
spec:
  mcp:
    targets:
      - name: grafana-mcp
        selector:
          services:
            matchLabels:
              app: grafana-mcp
