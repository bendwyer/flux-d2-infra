---
# yaml-language-server: $schema=https://raw.githubusercontent.com/CustomResourceDefinition/catalog/refs/heads/main/schema/onepassword.com/onepassworditem_v1.json
apiVersion: onepassword.com/v1
kind: OnePasswordItem
metadata:
  name: semaphore-oidc
  namespace: semaphore
spec:
  # 1Password item with field: client_secret (the tsidp OAuth client secret).
  # Mounted as a file so it never lands in the rendered config ConfigMap.
  itemPath: "vaults/4kbytax7eoweefgrkbg2af6oxu/items/7mkn5idpmf57itlniubgzmke3q"
