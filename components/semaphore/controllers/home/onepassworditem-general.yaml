---
# yaml-language-server: $schema=https://raw.githubusercontent.com/CustomResourceDefinition/catalog/refs/heads/main/schema/onepassword.com/onepassworditem_v1.json
apiVersion: onepassword.com/v1
kind: OnePasswordItem
metadata:
  name: semaphore-general
  namespace: semaphore
spec:
  # 1Password item with fields: cookieHash, cookieEncryption, accesskeyEncryption
  # (each a base64-encoded 32-byte value). These MUST stay stable across upgrades
  # or stored Semaphore secrets become undecryptable.
  itemPath: "vaults/4kbytax7eoweefgrkbg2af6oxu/items/lqwh5e7rlsgcucaw5wruvfpuhe"
