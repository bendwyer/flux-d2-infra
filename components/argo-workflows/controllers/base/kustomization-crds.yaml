---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: argo-workflows-crds
  namespace: argo-workflows
spec:
  interval: 1h
  path: manifests/base/crds/full
  prune: false
  wait: true
  sourceRef:
    kind: GitRepository
    name: argo-workflows
