---
# Identity for the health check Job that Omni creates between node upgrades. The Job
# manifest itself lives in the Omni config repo, since Omni delivers it, not Flux.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: omni-upgrade-gate
  namespace: omni-upgrade-gate
