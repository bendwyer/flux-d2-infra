---
apiVersion: collectors.grafana.com/v1alpha1
kind: Alloy
metadata:
  name: alloy-node
  namespace: alloy-node
spec:
  # spec mirrors the Alloy Helm chart's values file one-for-one.
  controller:
    type: daemonset
  rbac:
    create: true
    # The ClusterRole is rules plus clusterRules concatenated, and both carry
    # broad chart defaults covering components this collector does not run, so
    # both are overridden. Neither may be an empty list: the template renders
    # them in sequence and a literal [] in the middle is invalid YAML.
    rules:
      - apiGroups: [""]
        resources: [pods]
        verbs: [get, list, watch]
    clusterRules:
      - apiGroups: [""]
        resources: [nodes, nodes/metrics]
        verbs: [get, list, watch]
      - nonResourceURLs: [/metrics, /metrics/cadvisor]
        verbs: [get]
  alloy:
    # Node identity for the field selectors below. HOSTNAME is the pod name in a
    # pod, so the node name has to come from the downward API explicitly.
    extraEnv:
      - name: NODE_NAME
        valueFrom:
          fieldRef:
            fieldPath: spec.nodeName
    resources:
      limits:
        memory: 256Mi
      requests:
        cpu: 50m
        memory: 256Mi
