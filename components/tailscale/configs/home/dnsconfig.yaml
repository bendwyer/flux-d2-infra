---
# yaml-language-server: $schema=https://raw.githubusercontent.com/CustomResourceDefinition/catalog/refs/heads/main/schema/tailscale.com/dnsconfig_v1alpha1.json
apiVersion: tailscale.com/v1alpha1
kind: DNSConfig
metadata:
  name: ts-dns
spec:
  nameserver:
    image:
      repo: ghcr.io/tailscale/k8s-nameserver
      tag: v1.96.5
    service:
      clusterIP: 10.96.0.153
