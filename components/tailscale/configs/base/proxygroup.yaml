---
# yaml-language-server: $schema=https://raw.githubusercontent.com/CustomResourceDefinition/catalog/refs/heads/main/schema/tailscale.com/proxygroup_v1alpha1.json
apiVersion: tailscale.com/v1alpha1
kind: ProxyGroup
metadata:
  name: shared-ingress
spec:
  type: ingress
  replicas: 2
  hostnamePrefix: shared-ingress
  proxyClass: unprivileged
  tags:
    - tag:k8s-shared
---
# yaml-language-server: $schema=https://raw.githubusercontent.com/CustomResourceDefinition/catalog/refs/heads/main/schema/tailscale.com/proxygroup_v1alpha1.json
apiVersion: tailscale.com/v1alpha1
kind: ProxyGroup
metadata:
  name: private-ingress
spec:
  type: ingress
  replicas: 1
  hostnamePrefix: private-ingress
  proxyClass: unprivileged
  tags:
    - tag:k8s-private
---
# yaml-language-server: $schema=https://raw.githubusercontent.com/CustomResourceDefinition/catalog/refs/heads/main/schema/tailscale.com/proxygroup_v1alpha1.json
apiVersion: tailscale.com/v1alpha1
kind: ProxyGroup
metadata:
  name: egress
spec:
  type: egress
  replicas: 1
  hostnamePrefix: egress
  proxyClass: unprivileged
  tags:
    - tag:k8s-egress
