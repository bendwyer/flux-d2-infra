---
apiVersion: source.extensions.fluxcd.io/v1beta1
kind: ArtifactGenerator
metadata:
  name: k8up-crds
  namespace: k8up
spec:
  sources:
    - alias: chart
      kind: OCIRepository
      name: k8up
  artifacts:
    - name: k8up-crds
      # Tied to the chart's revision, so the CRDs move only when the chart does.
      originRevision: "@chart"
      copy:
        - from: "@chart/k8up/crds/**"
          to: "@artifact/"
