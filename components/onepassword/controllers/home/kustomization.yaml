---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../base
# No patches needed. The Connect credential and operator token are seeded by hand at
# bootstrap and on rotation, so they already exist by the time this reconciles.
